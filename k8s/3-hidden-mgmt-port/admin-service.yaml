apiVersion: v1
kind: Service
metadata:
  namespace: shoppingdemo
  name: admin-service
  labels:
    app: shoppingdemo
spec:
  ports:
    - name: "http"
      port: 8080
      targetPort: 8080
  selector:
    app: admin