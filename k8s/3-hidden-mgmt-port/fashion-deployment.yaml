apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: shoppingdemo
  name: fashion-pod
  labels:
    app: shoppingdemo
    type: spring-boot
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: fashion
    spec:
      containers:
      - image: joshiste/bestseller-fashion:chaos-ws-k8s
        imagePullPolicy: Never
        name: fashion
        env:
          - name: management.endpoints.web.exposure.include
            value: "*"
        ports:
        - containerPort: 8082
        resources: {}
        readinessProbe:
          httpGet:
            port: 8082
            path: /actuator/health
          initialDelaySeconds: 5
          failureThreshold: 3
          timeoutSeconds: 5
      restartPolicy: Always
  selector:
    matchLabels:
      app: fashion
